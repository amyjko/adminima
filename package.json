{
	"name": "adminima",
	"version": "0.8.10",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"test": "npm run test:integration && npm run test:unit",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"format": "prettier --plugin-search-dir . --write .",
		"test:integration": "playwright test",
		"test:unit": "vitest",
		"csv": "node csv2org.js",
		"start": "cp .env.local .env; supabase start --exclude storage-api,imgproxy,logflare,supavisor,vector",
		"stop": "cp .env.prod .env; supabase stop",
		"reset": "supabase db reset; npm run types",
		"diff": "supabase db diff --local --use-migra -f rename_me",
		"types": "supabase gen types typescript --local > src/database/database.types.ts",
		"outdated": "npx check-outdated --ignore-pre-releases --columns name,type,current,latest,changes"
	},
	"engines": {
		"node": ">=22"
	},
	"devDependencies": {
		"@playwright/test": "^1",
		"@supabase/supabase-js": "^2",
		"@sveltejs/kit": "^2",
		"@sveltejs/vite-plugin-svelte": "^7",
		"@types/uuid": "^10",
		"prettier": "^3",
		"prettier-plugin-svelte": "^3",
		"supabase": "^2",
		"svelte": "^5",
		"svelte-check": "^4",
		"tslib": "^2",
		"type-fest": "^5",
		"typescript": "^6",
		"vite": "^8",
		"vitest": "^4"
	},
	"type": "module",
	"dependencies": {
		"@supabase/ssr": "^0",
		"@sveltejs/adapter-vercel": "^6",
		"date-fns": "^4",
		"fast-csv": "^5",
		"uuid": "^13",
		"write-excel-file": "^4"
	}
}
